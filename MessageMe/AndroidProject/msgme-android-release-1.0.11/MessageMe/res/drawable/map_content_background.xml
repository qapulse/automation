<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <corners
        android:topLeftRadius="@dimen/radius_corner"
        android:topRightRadius="@dimen/radius_corner" />

    <!-- Make the background color the same as the message thread,
         otherwise the default black background looks ugly while
         waiting for map tiles to load. -->
    <solid android:color="@color/messages_background"/>
    
</shape>