package com.littleinc.MessageMe.util;

/**
 * Interface that will when the user 
 * has clicked in the mentor banner, so
 * the application can request the next mentor banner 
 * from the stack if any
 *
 */
public interface MentorBannerListener {

    public void onClickCompleted();

}
